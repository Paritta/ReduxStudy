import React from "react";

const propTypes = {
};

const defaultTypes = {
};

const Card = () => {
    return (
        <div>
            Card
        </div>
    );
};

Card.propTypes = propTypes;
Card.defaultTypes = defaultTypes;

export default Card;