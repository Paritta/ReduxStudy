import React from "react";

const propTypes = {
};

const defaultTypes = {
};

export const TopPopu = () => {
    return (
        <div>
        </div>
    )
};

TopPopu.propTypes = propTypes;
TopPopu.defaultTypes = defaultTypes;

export default TopPopu;
