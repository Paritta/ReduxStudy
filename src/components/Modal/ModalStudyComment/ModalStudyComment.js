import React from "react";
import PropTypes from "prop-types";

const propTypes = {
};

const defaultTypes = {
};

export const ModalStudyComment = () => {
    return (
        <div>
            ModalStudyComment
        </div>
    )
};

ModalStudyComment.propTypes = propTypes;
ModalStudyComment.defaultTypes = defaultTypes;

export default ModalStudyComment;
